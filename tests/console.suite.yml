# Codeception Test Suite Configuration

# suite for console tests.
# RUN `build` COMMAND AFTER ADDING/REMOVING MODULES.

class_name: ConsoleTester
modules:
    enabled:
        - Helper\Console
        - Helper\Category
        - Helper\Mail
        - Helper\User
        - Helper\Post
        - Filesystem
        - Asserts
        - Phalcon
        - Cli
